const express = require('express');
const cors = require('cors');
const app = express();

app.use(cors());

app.get('/', (req, res) => {
  res.send('服务正常：人机恋 APP 的后端已成功启动！');
});

app.get('/api', (req, res) => {
  res.send('服务正常：人机恋 APP 的后端已成功启动！');
});

module.exports = app;
